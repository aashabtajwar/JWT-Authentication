An authentication process made using Node.js and Express.js

It uses JWT as the authentication method and stores the token
in a cookie instead of the client having to send necessary
authorization headers with every request